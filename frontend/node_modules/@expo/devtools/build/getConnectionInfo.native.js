/**
 * Get the dev server address.
 */
import { PROTOCOL_VERSION } from './ProtocolVersion';
export function getConnectionInfo() {
    const _getDevServer = require('react-native/Libraries/Core/Devtools/getDevServer');
    const getDevServer = typeof _getDevServer === 'function' ? _getDevServer : _getDevServer?.default;
    const devServerObj = typeof getDevServer === 'function' ? getDevServer() : { url: 'http://localhost:8081/' };
    const devServer = devServerObj
        .url.replace(/^https?:\/\//, '')
        .replace(/\/?$/, '');
    return {
        protocolVersion: PROTOCOL_VERSION,
        sender: 'app',
        devServer,
    };
}
//# sourceMappingURL=getConnectionInfo.native.js.map